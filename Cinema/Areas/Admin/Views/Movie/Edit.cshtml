@model Movie

<div class="card p-4">
    <h4 class="mb-4">Edit Movie</h4>

    <!-- Edit Form -->
    <form asp-area="Admin" asp-controller="Movie" asp-action="Edit" method="post" enctype="multipart/form-data">
        @Html.AntiForgeryToken()
        <input type="hidden" name="Id" value="@Model.Id" />

        <div class="mb-3">
            <label class="form-label">Name</label>
            <input class="form-control" name="Name" value="@Model.Name" required />
        </div>
        <div class="mb-3">
            <label class="form-label">Upload Image</label>
            <input class="form-control" type="file" name="img" />
            @if (!string.IsNullOrEmpty(Model.MainImg))
            {
                <div class="mt-2">
                    <img src="@Model.MainImg" alt="@Model.Name" width="100" class="rounded shadow-sm" />
                </div>
            }
        </div>

        <div class="mb-3">
            <label class="form-label">Description</label>
            <textarea class="form-control" name="Description">@Model.Description</textarea>
        </div>

        <button class="btn btn-primary" type="submit">Save Changes</button>
    </form>

    <form asp-area="Admin" asp-controller="Actor" asp-action="Delete" asp-route-id="@Model.Id"
          method="post" onsubmit="return confirm('Are you sure you want to delete this actor?');"
          class="mt-3">
        <button type="submit" class="btn btn-danger">Delete Actor</button>
    </form>
</div>
